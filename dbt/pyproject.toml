[project]
name = "torneos-dbt"
version = "1.0.0"
description = "Proyecto dbt del Momento 3 — arquitectura Medallón sobre TORNEOS_DB en Snowflake"
requires-python = ">=3.11"
dependencies = [
    "dbt-core>=1.11,<2.0",
    "dbt-snowflake>=1.11,<2.0",
]
